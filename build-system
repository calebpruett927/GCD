[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "umcp"
version = "0.1.0"
description = "Generative Collapse Dynamics & Unified Monitoring pipeline"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pandas>=1.5",
    "numpy>=1.21",
    "click>=8.0",
    "rich>=12.0",
    "matplotlib>=3.5",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "flake8", "black", "ruff"]

[project.scripts]
umcp = "umcp.cli:cli"
